# Clé API Google Gemini (aistudio.google.com/apikey)
# Copier ce fichier en .env.local et renseigner la vraie valeur.
# Utilisée uniquement côté serveur (fonction api/chat.js) : jamais exposée au client.
GEMINI_API_KEY=
